/*!
 * oauth2 server
 * Copyright(c) 2012 Matti Savolainen <matti@applifier.com>
 * MIT Licensed
 */

/**
 * Module Dependencies
 */


/**
 * Authentication
 *
 * @param server  `Server` instance
 * @param options
 *
 * Options:
 *  `store`               The to be used, see store.js. defalts to local store.
 *
 */
exports = module.exports = function Authentication(server, options) {
  options = options || {};
}